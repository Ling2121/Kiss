return function(frames)
    --[[
        frames : 
            第一种形式:
                frames : {
                    image : "frames.png", //一张帧动画图 可以给予路径或者直接给予一张Image对象
                    speed : 0.5,//速度,
                    frame_width = 16,
                    frame_height = 16,
                    *frame_head = 1,
                    *frame_number = 3;
                }
    --]]

    local anim = {}

    function anim:draw()
        
    end
end